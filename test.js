const repo = require("./repo");

(async () => {
    try {
        // repo.create({ firstName: "Ryan", lastName: "Roman" });
        // repo.update("11668b82", {
        //     name: "Ryan",
        // });
    } catch (error) {
        console.log(error);
    }
})();
